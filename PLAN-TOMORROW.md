# План улучшений — 17 февраля

## Что сделано (16.02)
- Новый дизайн секции ролей: сравнительная таблица вместо карточек
- Backend: `match_score` (0-100), `reports_to`/`works_with`, `typical_duties` как строка
- Frontend: `RolesPanel` переписан как CSS Grid таблица с gauge-кольцами, column selection, count-бейджами
- Удалены: RecommendationBlock, RoleCard, RoleSelector, MatchLevelBadge
- Починены все 28 тестов (были сломаны ещё до моих изменений — неправильные имена полей)
- Протестировано на реальном резюме — LLM корректно отдаёт новую схему

## Что нужно доделать

### 1. Адаптив таблицы для мобильных (приоритет: высокий)
Таблица 960px не влезет на мобильный экран. Варианты:
- **A**: На < 768px переключать на вертикальные карточки (как было, но обновлённые)
- **B**: Горизонтальный скролл таблицы с залипшей первой колонкой (row labels)
- **C**: Tabs сверху + одна колонка на всю ширину (гибрид Variant 3)
- Рекомендую вариант **C** — он хорошо сочетается с текущим table layout

### 2. Анимация gauge при появлении (приоритет: средний)
Сейчас gauge рендерится сразу с финальным значением. Добавить:
- CSS transition на `stroke-dashoffset` при mount (уже есть в стиле, но нужен initial state = 0)
- Counter для числа процентов (0 → 85 за 0.8s)
- Можно через `useEffect` + `requestAnimationFrame`

### 3. Hover-эффекты на колонках (приоритет: низкий)
- При hover на ячейку — подсветить всю колонку (не только одну ячейку)
- Сделать через CSS-переменные или через state + onMouseEnter на grid column

### 4. Recommendation reasoning — куда деть? (приоритет: средний)
`recommendation.reasoning` по-прежнему приходит с бэкенда, но не отображается. Варианты:
- **Tooltip** при наведении на бейдж "Лучшее совпадение"
- **Expandable** секция под таблицей (collapse по умолчанию)
- **Убрать** из промпта совсем (экономия токенов ~200)
- Рекомендую **tooltip** — не загромождает, но инфо доступна

### 5. Стек тегов — пометить "уже умеет" vs "нужно выучить" (приоритет: средний)
Сейчас `typical_stack` — это инструменты роли, но не ясно, что из этого кандидат уже знает.
- Добавить в промпт: `matched_skills[]` и `missing_skills[]` вместо одного `typical_stack`
- На фронте: зелёная обводка для matched, серая для missing
- Или: иконка ✓/✕ рядом с тегом

### 6. Обновить live-тесты (приоритет: высокий)
`test_full_pipeline` в TestLiveAPI — обновить:
- Проверить новые поля `match_score`, `reports_to`, `works_with` в ответе roles
- Убрать проверку `data["score"]` из analyze (уже сделано, но нужен `score` отдельный endpoint)
- Добавить вызов `/api/tasks/{id}/score` и `/api/tasks/{id}/annotate` перед roles

### 7. Визуальный polish (приоритет: низкий)
- Попробовать subtle gradient на строках при hover (rgba подсветка)
- Добавить separator-линии между колонками (не везде, только headers)
- Тени на gauge-кольцах можно сделать чуть ярче для "высокое"
- Row labels: попробовать иконки (💪 сильные стороны, ⚠️ пробелы)

### 8. Баг: `create_mock_task` не ставит `annotations` (приоритет: средний)
Сейчас `create_mock_task()` не записывает `annotations` в storage, из-за чего
rewrite endpoint падает с 400 "Annotations not completed yet".
Нужно добавить `annotations=MOCK_DIAGNOSIS["sections"]` в `create_mock_task()`.
→ Уже пофикшено в test_rewrite_success, но не в create_mock_task — починить.
